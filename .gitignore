# Local remediation notes and progress tracking (never publish).
/docs/

# Environment-specific configuration and credentials. Keep sanitized examples.
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# macOS metadata should never enter the repository.
.DS_Store

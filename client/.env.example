# Copy to `.env` and adjust. Never commit `.env`.
#
# VITE_ variables are inlined into the built bundle and are therefore public.
# Do not put secrets here.

# API origin the browser calls at runtime.
# Local development: http://localhost:8000
# Production build:  https://api.khalidahammed.com
VITE_API_URL=http://localhost:8000
